# Projects

Isolated student / research projects. Each project gets its own directory
only when the work starts — do not keep empty placeholder directories.

Rules (see `docs/architecture.md`):

- May import from `core/`, `libs/`, and call public service APIs.
- Must not modify shared code or talk to hardware directly.
- Delete or archive the directory when the project ends.
