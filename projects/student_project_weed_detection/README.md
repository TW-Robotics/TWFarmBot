# student_project_weed_detection

Self-contained student project. Imports from `core/` and `libs/`; calls the
public APIs of `services/`. Does not modify shared code or talk to the
FarmBot hardware directly.
