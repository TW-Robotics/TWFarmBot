[project]
name = "farmbot-client"
version = "0.1.0"
description = "HTTP client for the local TWFarmbotOS REST API."
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["farmbot_client"]
